subscription WatchConversation($conversationId: ID!) {
  conversationChange(conversationId: $conversationId) {
    messages {
      id
      content
      insertedAt
      user {
        email
      }
    }
  }
}
