mutation SignUpMutation($email: String!, $password: String!) {
  signup(
    email: $email
    password: $password
    password_confirm: $password_confirm
  ) {
    token
    user {
      id
      email
    }
  }
}
