mutation SignUpMutation(
  $email: String!
  $password: String!
  $password_confirm: String!
) {
  signUp(
    email: $email
    password: $password
    password_confirm: $password_confirm
  ) {
    token
    user {
      id
      email
    }
  }
}
