mutation CreateMessageMutation($content: String!, $conversation_id: Number!) {
  create_message(content: $content, conversation_id: $conversation_id) {
    id
    content
    user {
      email
    }
  }
}
